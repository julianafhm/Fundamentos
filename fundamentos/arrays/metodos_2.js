const numeros = [1, 2, 3, 4, 5, 8];

numeros.splice(1, 2); // remove os elementos do array (indice, quantidade)
console.log(numeros);

numeros.pop() //remove o ultimo elemento do array cada vez que é chamado, do final pro inicio
console.log(numeros);//no console.log é exibido o que foi excluído

console.log(numeros.pop());
console.log(numeros.pop());


console.log(numeros);


